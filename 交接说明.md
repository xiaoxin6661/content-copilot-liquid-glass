# Content Copilot 项目交接说明

给接手继续开发的人（或 Trae/其他 AI 编程工具）看的完整上下文，确保工作能无缝衔接。

---

## 项目是什么

**Content Copilot** —— Sigma 跑步 App 达人合作全流程管理工具。液态玻璃（liquid glass）视觉风格，纯前端 HTML/CSS/JS，无框架无后端，数据存浏览器 localStorage。

- **源码仓库**（公开）：https://github.com/xiaoxin6661/content-copilot-liquid-glass
- **在线可用网页（GitHub Pages）**：https://xiaoxin6661.github.io/content-copilot-liquid-glass/
- **内部部署（小红书内网，公司环境才能开）**：https://cowork.xiaohongshu.com/f/content-copilot-liquid-glass/
- 当前版本：**v2.12.1**（写在 `.cowork.json` 里，GitHub 上这个文件已被剔除，见下方约束）

---

## 关键决定（Key Decisions）

1. **侧栏从 6 个模块精简为 4 个**：达人管理 / 达人清单 / 内容审核 / Brief 设置。原因：参照公司内部另一个 Content Copilot 参考应用（`cowork.xiaohongshu.com/s/content-copilot/dashboard`）的结构做的对齐。**去掉了「工具中心」（launch.html）和「笔记分析」（analysis.html）的侧栏入口**——这两个文件还留在项目里，只是不在导航里了。
2. **不接入雅典娜数据后台**：用户明确要求脱离公司内网数据源，改为达人管理/内容审核走纯规则引擎（不需要数据），投流优化那一环理论上该接"前台公开数据自动同步（赞/藏/评，每6小时）"，但**这部分尚未实现**（见下方未解决问题）。
3. **催稿话术固定为单一文案**，取消随机变体池：
   > 哈喽老师，同步确认下初稿进度，辛苦抽空发我一下，谢谢～
4. **今日待办按负责人（owner）过滤**：默认显示"我的"，可切"全部"看团队。新建合作自动填当前操作人为 owner。
5. **模版库读写打通**：之前笔记分析入库写 `cc_templates`，但工具中心读的是 `data.js` 静态数据，两边不通——已修复为"localStorage 优先，merge 静态默认数据"。
6. **模版删除支持所有来源**：手动添加的直接删；静态默认模版走"黑名单"机制（加入黑名单，刷新后不再显示，不是真删除原始数据）。
7. **审核后可沉淀内容资产**：审核通过面板新增"沉淀口播"（存完整正文）和"沉淀CTA"（自动取正文最后两句）两个按钮，直接进模版库。
8. **达人卡片 UID 可点击跳转小红书主页**：`xiaohongshu.com/user/profile/{UID}`；今日待办卡片里的达人名字同样可跳转。
9. **"工具中心"待办点击后自动推进阶段**：从"已发布"点"开始投流"后自动推进到"投流中"，同时从待办消失，跳转工具中心页。
10. **GitHub 仓库设为公开**（用户明确确认），已剔除内网元数据 `.cowork/`、`.cowork.json` 不上传公开仓库。
11. **GitHub Pages 已开启**，公开仓库可以免费用 Pages，用户离职后仍可访问和维护。

---

## 未解决问题（Open Issues）

1. **"投流优化 + 数据沉淀"功能未落地**——用户提出想法（发布后给优化建议 + 根据数据沉淀可复用模版/话术），讨论出方案是"每6小时自动同步前台公开互动数据（赞/藏/评，拿不到曝光/CTR/涨粉）"，但**代码还没写**，卡在"抓一条真实笔记做实测"这一步（已实测过一条链接，审核逻辑跑通了，但自动同步的定时抓取机制没做）。
2. **雅典娜替代方案最终没有做技术选型收尾**——讨论了粘贴表格/截图OCR/CSV导入/前台链接解析等多个方案，用户最后问"能不能接入其他工具"，答案是"没有官方授权账号+要在外部用+要长期自动同步"这个组合下技术上做不到官方数据，只能退化成"前台公开数据"，**这条结论用户已确认接受，但功能未实现**。
3. **达人示例数据是否需要脱敏**——AI 主动提出"要不要把达人示例数据换成脱敏假数据再推一版"，用户还没回复确认。**目前公开仓库里的达人数据是否含真实达人信息，需要人工核实一遍再决定。**
4. **本地 git 历史和 GitHub 远程有一次分叉**：因为一次 API 直传文件（`离职后维护指南.md`）绕开了本地 git push（因为代理隧道多次502超时），远程比本地多一次 commit，本地未做 `git pull`/`git merge` 同步。**下次在本地 clone 前建议先确认远程是最新状态，避免冲突**。
5. **抓取"批量优质笔记入库"的任务被中断**——用户提议"抓雅典娜/前台优质笔记入库文档库"，AI 提出方案 A（用户发链接）+ 方案 B（自动搜索公开高赞笔记），用户还没选，也没开始执行，中途被"给我压缩包"的请求打断，**这个任务处于挂起状态**。

---

## 已修改的文件（Modified Files，按最近顺序）

在 `cowork/content-copilot-liquid-glass/` 目录下：

- `index.html`（达人管理：侧栏改4项、今日待办owner过滤、待办名字可跳转小红书主页、工具中心待办自动推进阶段）
- `creators.html`（**新建**：达人清单独立页，复用达人看板逻辑，去掉待办部分）
- `review.html`（内容审核：修过一次"文件末尾缺 `</script></body></html>` 导致全页白屏"的严重bug；新增沉淀口播/CTA按钮；侧栏改4项）
- `briefs.html`（**新建**：Brief设置独立页，把原来侧栏抽屉里的Brief展示内容改成独立页面，支持PDF导入/恢复官方）
- `assets/main.js`（侧栏nav结构改4项；模版删除黑名单机制；owner过滤trim对比；UID点击跳转逻辑）
- `assets/data.js`（模版库新增Cyndi那条笔记提炼出的标题模版和口播话术模版）
- `analysis.html`（笔记分析页，仍存在但已从侧栏移除）
- `launch.html`（工具中心页，仍存在但已从侧栏移除，模版库读写逻辑在这里改的）
- `离职后维护指南.md`（**新建**：Windows+Cursor本地开发全流程文档）
- `交接说明.md`（本文档）
- `.gitignore`、剔除 `.cowork/`、`.cowork.json`（内网部署元数据，不进公开仓库）

**已知严重bug教训**：`review.html` 曾经文件末尾缺少 `</script>\n</body>\n</html>` 三行，导致浏览器把整个body当成未闭合script解析，页面完全白屏，报错显示"函数未定义"这类误导性信息。**改任何 `.html` 文件后务必检查文件末尾闭合标签是否完整**，这是本项目最容易踩的坑。

---

## 已确认约束（Confirmed Constraints）

1. **不调用雅典娜内网API**——用户明确要求，且离职后要在公司外部使用，内网系统本来就用不了。
2. **不做爬虫抓小红书后台数据**——AI主动声明"不会帮做爬虫方向"，涉及平台规则和账号风险。
3. **公开数据只能拿到点赞/收藏/评论/分享，拿不到曝光/CTR/完播率/涨粉数**——这是平台层面的硬限制，不是技术能力问题。
4. **GitHub仓库最终定为公开**——用户在"私有"和"公开"之间反复过（先私有→后为了开Pages改公开），当前状态是**公开**，用户已知晓风险（Brief规则/话术/达人数据全网可见）。
5. **HTML文件末尾必须保持闭合**（`</script></body></html>`），否则整页白屏。
6. **localStorage key 统一前缀 `cc_`**（`cc_collabs`、`cc_templates`、`cc_customBrief`等）。
7. **owner字段比较必须 `.trim()`**，避免登记表导入的数据带空格导致过滤失效。
8. **所有资源引用版本号统一**（当前 `?v=2400`），改动静态资源后要同步bump版本号避免CDN缓存问题。

---

## 下一步动动作（Next Actions，按优先级）

1. **【高优先级】决定达人示例数据是否需要脱敏**，公开仓库里如果有真实达人信息，应该先处理再继续对外分享链接。
2. **【中优先级】落地"投流优化+数据沉淀"功能**：
   - 技术方案已定：前台公开数据（赞/藏/评）+ 手动粘贴/填表兜底
   - 需要做：达人清单加"笔记链接"字段 → 数据快照存储（带时间戳）→ 抓取逻辑（浏览器渲染读取公开页面数字，不能用简单HTTP抓取）→ 投流优化建议规则 → 沉淀判断逻辑
3. **【中优先级】批量优质笔记入库**：用户要选定"发链接给AI批量提炼"还是"AI自动搜索公开高赞笔记"，或两者都做，然后执行提炼入库到模版库。
4. **【低优先级】本地环境搭建**：按照 `离职后维护指南.md` 装 Git + Cursor，clone仓库到本地Windows电脑，之后所有功能开发在本地用Cursor完成，push回GitHub，Pages自动更新。
5. **【低优先级】同步本地git历史**：clone最新远程仓库到本地前，注意远程比之前讨论时的本地状态多一次commit（`离职后维护指南.md`是走API直传的，不是git push），正常clone不会有问题，只是提醒別再手动合并旧的本地副本。

---

## 给 Trae（或任何AI编程工具）接手时的建议开场

把这份文档 + 上面提到的 `离职后维护指南.md` 一起丢给新的AI工具读，再说明：
> "我要继续开发这个项目，先接哪个未解决问题看你建议，我倾向先做 XXX"

这样新工具能直接对齐上下文，不需要重新摸索项目结构。
