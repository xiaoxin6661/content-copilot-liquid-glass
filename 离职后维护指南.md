# 离职后如何继续维护这个项目（Windows 版）

这份文档教你在**没有公司环境、没有 AI 助手帮你操作**的情况下，自己在 Windows 电脑上继续改这个项目的代码逻辑，改完自动更新到线上网页。

---

## 你现在拥有什么

- **代码仓库**：https://github.com/xiaoxin6661/content-copilot-liquid-glass （你自己的账号，永久属于你）
- **在线网页**：https://xiaoxin6661.github.io/content-copilot-liquid-glass/ （任何人都能直接打开用）
- 这两个东西完全独立于公司环境，离职后照样能用，不会失效。

**原理**：你改本地文件 → 推送（push）到 GitHub → GitHub 自动重新生成网页，1-2 分钟后线上更新。全程免费，不需要服务器。

---

## 第一步：装两个软件（一次性，10 分钟）

### 1. 装 Git（用来上传代码）
1. 打开 https://git-scm.com/download/win
2. 下载后一路点「Next」安装（默认选项就行，不用改）
3. 装完后，桌面右键会多一个「Git Bash Here」选项

### 2. 装 Cursor（AI 编程工具，中文友好，能帮你改代码逻辑）
1. 打开 https://cursor.com
2. 下载 Windows 版，安装
3. 打开后用邮箱注册登录（免费版够用）

> Cursor 就是一个能看懂代码、能对话着帮你改代码的编辑器，类似你现在用的 AI 助手，但装在你自己电脑上，不需要公司环境。

---

## 第二步：把代码下载到自己电脑（一次性）

1. 在电脑上新建一个文件夹，比如 `D:\我的项目`
2. 在这个文件夹里点右键 → 「Git Bash Here」，会弹出一个黑色命令窗口
3. 粘贴下面这行，回车：

```bash
git clone https://github.com/xiaoxin6661/content-copilot-liquid-glass.git
```

4. 等它跑完，`D:\我的项目` 里会出现一个 `content-copilot-liquid-glass` 文件夹，里面就是全部代码

---

## 第三步：用 Cursor 打开项目改代码

1. 打开 Cursor
2. 「File」→「Open Folder」→ 选中 `content-copilot-liquid-glass` 这个文件夹
3. 左侧能看到所有文件：
   - `index.html` = 达人管理页
   - `creators.html` = 达人清单页
   - `review.html` = 内容审核页
   - `briefs.html` = Brief 设置页
   - `assets/main.js` = 通用逻辑（侧栏、达人卡片渲染、待办逻辑）
   - `assets/data.js` = 数据（达人示例、模版库、Brief 规则）
   - `assets/review.js` = 审稿打分逻辑

4. **改功能逻辑的方式**：按键盘 `Ctrl + L`（或者点右下角对话图标），像跟我现在聊天一样，直接跟 Cursor 说你想改什么，比如：
   > 「今日待办的排序改成按逾期天数从多到少排」

   Cursor 会自己找到对应代码并改好，你看一下改动，觉得没问题就接受（Accept）。

5. **本地看效果**：改完直接双击 `index.html`（或者右键用浏览器打开），就能在浏览器里看到改动效果，不用等发布。

---

## 第四步：确认没问题后，推送到线上

改完并且在本地浏览器验证过没问题，回到 Git Bash 窗口（在项目文件夹里右键「Git Bash Here」），依次输入：

```bash
git add -A
git commit -m "说明这次改了什么"
git push
```

第一次 push 时会让你登录 GitHub 账号（弹出浏览器授权），登录你自己的 xiaoxin6661 账号即可，之后不用再登录。

**推送后等 1-2 分钟**，刷新线上网址就能看到更新：
https://xiaoxin6661.github.io/content-copilot-liquid-glass/

---

## 常见问题

**Q: 改错了想撤销怎么办？**
在 Git Bash 里输入：
```bash
git checkout -- .
```
会撤销所有未提交的改动，恢复到上次 push 的状态。

**Q: 想找回之前的某个版本？**
```bash
git log --oneline
```
能看到所有历史版本，每条前面一串字符是版本号，找到想要的版本号后问 Cursor「帮我恢复到 xxxx 这个版本」即可。

**Q: 想让 Cursor 帮我做更大的改动，它需要知道项目背景吗？**
可以把这份文档、以及下面「项目背景速览」直接贴给 Cursor 的对话框，它读完就有上下文了。

**Q: 网页打不开了/白屏了怎么办？**
最常见原因是某个 `.html` 文件末尾少了 `</script></body></html>` 这三行标签（本项目之前踩过这个坑）。让 Cursor 检查一下有没有文件末尾标签缺失。

---

## 项目背景速览（给 Cursor 用，也给未来的你自己看）

**这是什么**：Content Copilot，一个达人合作全流程管理工具，服务于「Sigma 跑步 App」的达人内容运营，覆盖：达人管理（催稿待办）→ 内容审核（Brief 合规检查+爆款潜力评分）→ Brief 设置（合作规则配置）。

**技术栈**：纯前端 HTML/CSS/JS，无框架无后端，数据存在浏览器 localStorage。液态玻璃（liquid glass）视觉风格。

**四个页面**：
- `index.html` 达人管理：今日待办 + 达人看板，按阶段（待初稿/初稿已确认/待发布/推流中/已归档）推进
- `creators.html` 达人清单：全量达人列表 + 阶段筛选
- `review.html` 内容审核：两步式审核（Brief 合规 + 爆款潜力打分），支持沉淀口播话术/CTA 钩子
- `briefs.html` Brief 设置：查看/导入合作规则（支持从 PDF 导入）

**核心数据结构**（在 `assets/data.js`）：
- `CC_DATA.collabs`：达人合作列表（每条含 owner 负责人、阶段 stage、UID、主页链接等）
- `CC_DATA.brief`：官方 Brief 规则（必带元素/硬性规则/12大功能/流量激励/违禁项）
- `CC_DATA.templates`：模版库（标题/口播话术/封面/CTA）

**关键约定**：
- 所有 `.html` 文件末尾必须是 `</script>\n</body>\n</html>`，缺一行都会导致整页白屏
- localStorage 里的数据 key 前缀是 `cc_`（如 `cc_collabs`、`cc_templates`、`cc_customBrief`）
- `owner` 字段做比较时要 `.trim()`，避免空格导致过滤失效

---

祝你顺利！这个项目完全是你的，随时可以继续改、继续用。
